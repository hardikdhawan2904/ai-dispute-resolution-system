# CCA is a linear generate-and-deliver pipeline, not a ReAct loop.
# No LangChain tools are needed — generation happens in the LLM call node
# and delivery happens in the deliver node via email_service.

